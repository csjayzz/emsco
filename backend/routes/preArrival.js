// Pre-arrival routes are mounted under /rides/:id/pre-arrival
// See routes/rides.js for the actual route definitions.
// This file exists for structural completeness.

const router = require('express').Router();
module.exports = router;
